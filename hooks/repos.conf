#!/bin/bash
# repos.conf -- Tracked repositories shared list
# Referenced by: bash-guard.sh / track-edit.sh / auto-commit.sh
#
# CUSTOMIZE: Add your own repo paths below.
# These repos will be monitored for uncommitted changes at session end.

TRACKED_REPOS=(
  # === Add your repos here ===
  # "$HOME/my-project"
  # "$HOME/Projects/another-project"
  # "$HOME/work/company-repo"
)
